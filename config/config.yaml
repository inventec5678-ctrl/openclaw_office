# OpenClaw Office 配置

server:
  port: 3000
  host: "0.0.0.0"

openclaw:
  gateway: "http://127.0.0.1:18789"
  apiBase: "/api"
  pollInterval: 5000  # 毫秒

sse:
  heartbeatInterval: 30000  # SSE 心跳間隔（毫秒）
